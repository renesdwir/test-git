const test = "testing dimulai dari sini";

const newFeature = "renes";

function consoleText(text) {
  for (let i = 0; i < text.length; i++) {
    console.log(text[i]);
  }
}

consoleText(newFeature);
